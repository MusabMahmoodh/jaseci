- **Fix: A declared dependency that is not installed is an error**: An unresolved import of a package declared in `jac.toml` `[dependencies]` is now `E1136` instead of warning `W1100`, since every type flowing from it went unchecked; optional imports under `try`/`except ImportError` keep the warning.
